<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteRule ^(config|database|src|views)(/|$) - [F,L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>

Options -Indexes
